extends layout

block content
  .main.container.text-xs-center
    h1.display-3 Weather
    p.lead Let Check the Weather
    a.btn.btn-info-outline.btn-lg(href='/register')  FREE for register

    #{Weather}